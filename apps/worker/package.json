{
  "name": "careerpilot-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker BFF API for CareerPilot AI",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "kv:create": "wrangler kv:namespace create ROADMAP_CACHE && wrangler kv:namespace create RATE_LIMIT"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.0"
  }
}
